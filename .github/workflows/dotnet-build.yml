name: dotnet-build.yml

on:
  push:
    branches: main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo Hello from dotnet-buils.yml

      - name: Sponsor Labels
        uses: JasonEtco/is-sponsor-label-action@v2.0.0